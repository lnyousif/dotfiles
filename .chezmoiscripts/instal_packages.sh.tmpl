  DNF_CMD=$(which yum)
  APT_GET_CMD=$(which apt-get)
  
if [[ ! -z $DNF_CMD ]]; then
    dnf install $YUM_PACKAGE_NAME
 elif [[ ! -z $APT_GET_CMD ]]; then
    apt-get $DEB_PACKAGE_NAME
 else
    echo "error can't install package $PACKAGE"
    exit 1;
 fi