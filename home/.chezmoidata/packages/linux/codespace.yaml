packages:
  codespace:
    linux:
      common:
        apt:
        apt_junk:
        pips:
        linux_cmd:
          - echo "Setting up DevContainer environment..."
          - if [ -f "./setup-project.sh" ]; then bash ./setup-project.sh; fi
