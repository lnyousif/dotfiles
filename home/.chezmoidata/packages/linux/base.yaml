packages:
  base:
    linux:
      common:
        apt:
          - git
          - curl
          - python3-pip
          - neovim
        apt_url:
        apt_junk:
          - firefox
          - libreoffice-data
          - gnome-boxes
          - gnome-maps
          - gnome-terminal
          - gnome-tour
          - gnome-text-editor
          - moby-engine
        dra:
          package:
          appimage:
          bin:
        kde:
        tablet:
        pips:
          - commitizen
          - pre-commit
        linux_cmd:
          - curl -sS https://starship.rs/install.sh | sh
