packages:
  base:
    linux:
      common:
        apt:
          - git
          - age
          - expect
          - curl
          - wget
          - python3-pip
          - pipx
          - micro
          - tmux
          - xclip
          - keepassxc
          - nn
        apt_junk:
          - firefox
          - libreoffice-data
          - gnome-boxes
          - gnome-maps
          - gnome-terminal
          - gnome-tour
          - gnome-text-editor
          - moby-engine
          - calendar
          - calculator
        pips:
        linux_cmd:
          - curl -sS https://starship.rs/install.sh | sh -s -- -y
