packages:
  linux:
    curls:
      - "curl -s https://ohmyposh.dev/install.sh | sudo bash -s -- -d ~/bin"
    dnfs:
      - "git"
    apts:
      - "docker-desktop"
  darwin:
    brews:
      - "git"
    casks:
      - "google-chrome"