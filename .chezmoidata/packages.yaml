packages:
  darwin:
    brews:
      - "git"
    casks:
      - "google-chrome"
  linux:
    dnfs:
      - "git"
    apts:
      - "docker-desktop"
    curls:
      - "curl -s https://ohmyposh.dev/install.sh | sudo bash -s -- -d ~/bin"
